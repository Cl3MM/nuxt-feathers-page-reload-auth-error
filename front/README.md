# Nuxt & Feathers Not Authenticated on page reload Error

## Build Setup

```javascript
# terminal 1
$ cd front && yarn install
$ yarn run dev

# terminal 2
$ cd server && yarn install
$ yarn run dev
```
